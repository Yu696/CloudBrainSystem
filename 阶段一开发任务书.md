# 云脑诊疗平台 — 阶段一开发任务书

> 阶段目标：搭建单体架构，完成 5 个核心模块（M01/M02/M03/M06/M08）的前后端 CRUD 开发

---

## 一、任务概览

| 任务编号 | 任务名称 | 涉及模块 | 估计人天 |
|---------|---------|---------|---------|
| T1 | 基础设施搭建 | — | 0.5 |
| T2 | 后端项目脚手架 | — | 1 |
| T3 | 前端项目脚手架 | — | 1 |
| T4 | M01 用户中心模块开发 | M01 | 1.5 |
| T4.5 | M08 系统管理模块开发 | M08 | 1 |
| T5 | M02 患者档案模块开发 | M02 | 1.5 |
| T6 | M06 预约管理模块开发 | M06 | 3 |
| T7 | M03 诊疗记录模块开发 | M03 | 2.5 |
| T8 | 前后端联调 | 全部 | 1.5 |
| T9 | 部署验证 | — | 1 |

---

## 二、各任务详细说明

---

### T1 基础设施搭建

**目标：** Linux 虚拟机 MySQL + Redis + MinIO 就绪

**清单：**
1. Linux 安装 Docker + Docker Compose
2. 上传 `docker-compose.yml` 至 `/home/yuu/CloudBrain/`
3. `docker compose up -d` 启动服务
4. 验证运行状态

```bash
docker compose ps
# 应看到 3 个服务均为 Up

# 验证 MySQL
mysql -h 192.168.110.128 -u root -proot123456 -e "SHOW DATABASES;"

# 验证 Redis
redis-cli -h 192.168.110.128 -a redis123456 ping
# 应返回 PONG

# 验证 MinIO
# 浏览器访问 http://192.168.110.128:9001 登录 minioadmin/minioadmin123
```

5. SQL脚本：放在 `/home/yuu/CloudBrain/sql/init/` 后重启容器自动执行

```bash
# 方法一：重启容器自动执行（文件在 docker-entrypoint-initdb.d）
docker compose restart mysql

# 方法二：手动执行（推荐，可控）
mysql -h 192.168.110.128 -u root -proot123456 < sql/init/00-init-db.sql
mysql -h 192.168.110.128 -u root -proot123456 cloudbrain < sql/init/01-user.sql
# ... 依次执行 02-08.sql
```

**产物：** 3 个容器运行正常，cloudbrain 数据库 40 张表创建成功

---

### T2 后端项目脚手架（Spring Boot 3）

**目标：** 可启动的 Spring Boot 3 单体项目

**技术依赖：**
```
Spring Boot 3.2.x
Spring Web (spring-boot-starter-web)
Spring Data JPA (spring-boot-starter-data-jpa)
MySQL Driver
Lombok
Validation (spring-boot-starter-validation)
Spring Security (可选, 用于 JWT)
Knife4j (swagger3 接口文档)
```

**任务分解：**

| 步骤 | 内容 |
|------|------|
| 2.1 | 使用 Spring Initializr 创建项目（或手动搭建） |
| 2.2 | 配置 `application.yml`：MySQL 连接、JPA、Redis、MinIO |
| 2.3 | 集成 Knife4j（访问 `/doc.html` 查看 API 文档） |
| 2.4 | 配置 CORS（允许前端 5173 端口跨域） |
| 2.5 | 编写全局统一响应 `Result<T>` 泛型类 |
| 2.6 | 编写全局异常处理器 `GlobalExceptionHandler` |
| 2.7 | 集成 JWT 工具类（生成/验证 token） |
| 2.8 | 集成 Redis 配置（缓存 token、用户信息） |

**`application.yml` 核心配置：**
```yaml
server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://192.168.110.128:3306/cloudbrain?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai
    username: root
    password: root123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: 192.168.110.128
      port: 6379
      password: redis123456

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /api-docs

knife4j:
  enable: true
```

**项目包结构：**
```
com.cloudbrain
├── CloudBrainApplication.java
├── common/            # 统一响应、异常处理、常量
│   ├── Result.java
│   ├── GlobalExceptionHandler.java
│   └── BaseController.java
├── config/            # 配置类
│   ├── CorsConfig.java
│   ├── Knife4jConfig.java
│   └── RedisConfig.java
├── security/          # JWT 安全
│   ├── JwtUtil.java
│   ├── JwtAuthFilter.java
│   └── SecurityConfig.java
├── controller/        # 按模块分包
│   ├── user/
│   ├── system/        # M08 系统管理
│   ├── patient/
│   ├── appointment/
│   └── medical/
├── service/           # 业务逻辑
│   ├── user/
│   ├── system/        # M08 系统管理
│   ├── patient/
│   ├── appointment/
│   └── medical/
├── repository/        # JPA 数据访问
├── entity/            # 实体类
└── dto/               # 数据传输对象
```

**产物：** `mvn spring-boot:run` 可正常启动，访问 `http://localhost:8080/doc.html` 显示 Swagger 页面

---

### T3 前端项目脚手架（Vue 3 + TS）

**目标：** 可启动的 Vue 3 前端项目

**技术依赖：**
```
Vue 3 + TypeScript
Vite
Element Plus
Axios
Vue Router
Pinia
```

**任务分解：**

| 步骤 | 内容 |
|------|------|
| 3.1 | `npm create vite@latest frontend -- --template vue-ts` |
| 3.2 | 安装依赖：`npm install element-plus axios vue-router pinia @element-plus/icons-vue` |
| 3.3 | Axios 封装（`utils/request.ts`）：请求拦截器（带 JWT）+ 响应拦截器（统一错误处理） |
| 3.4 | `vite.config.ts` 配置代理（`/api` → `http://localhost:8080`） |
| 3.5 | 配置 Vue Router + Pinia |
| 3.6 | 配置 Element Plus 全局引入 |
| 3.7 | 创建基本布局：侧边导航 + 顶部栏 + 内容区 |

**`request.ts` 核心：**
```typescript
const request = axios.create({ baseURL: '/api', timeout: 30000 })

// 请求拦截器：自动携带 JWT
request.interceptors.request.use(config => {
  const token = localStorage.getItem('token')
  if (token) config.headers.Authorization = `Bearer ${token}`
  return config
})

// 响应拦截器：统一错误处理
request.interceptors.response.use(
  res => res.data,
  err => { ElMessage.error(err.response?.data?.message || '请求失败'); return Promise.reject(err) }
)
```

**项目目录：**
```
frontend/
├── src/
│   ├── api/              # 接口封装（按模块分）
│   │   ├── user.ts
│   │   ├── patient.ts
│   │   ├── system.ts
│   │   ├── appointment.ts
│   │   └── medical.ts
│   ├── views/            # 页面组件
│   │   ├── system/       # M08 系统管理
│   │   ├── user/
│   │   ├── patient/
│   │   ├── appointment/
│   │   └── medical/
│   ├── router/
│   ├── stores/           # Pinia
│   │   └── user.ts       # 用户登录状态
│   ├── components/       # 公共组件
│   ├── utils/
│   │   ├── request.ts
│   │   └── auth.ts
│   └── types/            # TS 类型定义
├── vite.config.ts
└── package.json
```

**产物：** `npm run dev` 可正常启动，访问 `http://localhost:5173` 显示布局页面

---

### T4 M01 用户中心模块（Controller + Service + Repository + 前端页面）

**后端 API 清单：**

| 方法 | 路径 | 说明 | 请求参数 | 返回 |
|------|------|------|---------|------|
| POST | `/api/user/login` | 医生/管理员登录 | `{username, password}` | `{token, userInfo}` |
| POST | `/api/user/register` | 用户注册 | `{username, password, realName, phone, userType}` | `{userId}` |
| GET | `/api/user/info` | 获取当前用户信息 | Header: `Authorization: Bearer {token}` | `{userId, username, realName, role, ...}` |
| PUT | `/api/user/update` | 更新个人信息 | `{realName, phone, email}` | `{success}` |
| PUT | `/api/user/reset-password` | 重置密码 | `{oldPassword, newPassword}` | `{success}` |
| POST | `/api/role/assign` | 分配角色 | `{userId, roleId}` | `{success}` |
| GET | `/api/role/permissions` | 查询角色权限 | `roleId` | `[{permission}]` |
| GET | `/api/role/user-role` | 查询用户角色 | `userId` | `{roleId, roleName}` |
| PUT | `/api/role/update-permission` | 更新角色权限 | `{roleId, permissionIds}` | `{success}` |
**数据表：** `user`, `role`, `user_role`, `permission`, `role_permission`, `login_log`

**前端页面：**

| 页面 | 路由 | 对应组件 | 说明 |
|------|------|---------|------|
| 登录 | `/login` | LoginView | 用户名/密码 + 角色选择 + JWT 存储 |
| 注册 | `/register` | RegisterView | 注册表单 + 短信验证码（模拟） |
| 个人信息 | `/profile` | ProfileView | 查看/编辑个人信息 |
| 角色管理 | `/admin/role` | RoleManageView | 角色 CRUD（管理员） |
| 权限管理 | `/admin/permission` | PermissionView | 权限配置（管理员） |

**关键逻辑：**
- 登录后 JWT 存 `localStorage` + Pinia
- Axios 拦截器自动携带 Token
- 角色权限控制（路由守卫 `router.beforeEach`）

---

### T4.5 M08 系统管理模块（Controller + Service + Repository + 前端页面）

**后端 API 清单：**

| 方法 | 路径 | 说明 | 请求参数 | 返回 |
|------|------|------|---------|------|
| POST | `/api/system/user/add` | 新增系统用户 | `{username, password, roleId}` | `{userId}` |
| GET | `/api/system/user/list` | 系统用户列表 | — | `[{user}]` |
| GET | `/api/system/user/detail` | 用户详情 | `userId` | `{user, role}` |
| PUT | `/api/system/user/update` | 修改用户 | `{userId, username, roleId}` | `{success}` |
| PUT | `/api/system/user/status` | 启用/禁用 | `{userId, status}` | `{success}` |
| DELETE | `/api/system/user/delete` | 删除用户 | `userId` | `{success}` |

**数据表：** `system_user`

**前端页面：**

| 页面 | 路由 | 对应组件 | 说明 |
|------|------|---------|------|
| 管理员登录 | `/admin/login` | AdminLoginView | 管理员独立登录入口 |
| 用户列表 | `/admin/users` | UserManageView | 系统用户表格展示 + 搜索 + 状态管理 |
| 新增用户 | `/admin/users/add` | UserAddView | 弹窗/页面表单，填写用户名、角色等 |

**关键逻辑：**
- M08 管理员操作需要验证当前用户是否为管理员角色
- 删除用户时校验是否有关联数据（角色、业务记录）

---

### T5 M02 患者档案模块（Controller + Service + Repository + 前端页面）

**后端 API 清单：**

| 方法 | 路径 | 说明 | 请求参数 | 返回 |
|------|------|------|---------|------|
| POST | `/api/patient/create` | 创建患者档案 | `{name, idCard, gender, phone, ...}` | `{patientId, medicalRecordNo}` |
| GET | `/api/patient/info` | 查询患者信息 | `patientId` | `{patient}` |
| GET | `/api/patient/list` | 患者列表 | `name/phone/medicalRecordNo` | `[{patient}]` |
| PUT | `/api/patient/update` | 更新患者档案 | `{patientId, ...}` | `{success}` |
| GET | `/api/patient/check-idcard` | 校验身份证 | `idCard` | `{exists: true/false}` |

**数据表：** `patient`

**前端页面：**

| 页面 | 路由 | 对应组件 | 说明 |
|------|------|---------|------|
| 新建档案 | `/patient/create` | PatientCreateView | 患者信息录入表单（带身份证校验） |
| 患者列表 | `/patient/list` | PatientListView | 搜索 + 表格展示 |
| 档案详情 | `/patient/detail/:id` | PatientDetailView | 查看/编辑档案详情 |

---

### T6 M06 预约管理模块（Controller + Service + Repository + 前端页面）

> **分工说明：** 余润东负责后端 API（12 个），杨继鑫负责前端页面（6 个）

**后端 API 清单：**

| 方法 | 路径 | 说明 | 请求参数 | 返回 |
|------|------|------|---------|------|
| GET | `/api/department/list` | 科室列表 | — | `[{department}]` |
| GET | `/api/doctor/list` | 医生列表 | `departmentId`(可选) | `[{doctor}]` |
| GET | `/api/doctor/detail` | 医生详情 | `doctorId` | `{doctor}` |
| POST | `/api/schedule/set` | 设置排班 | `{doctorId, date, shift, ...}` | `{scheduleId}` |
| GET | `/api/schedule/query` | 查询排班 | `doctorId, startDate, endDate` | `[{schedule}]` |
| GET | `/api/schedule/available` | 可用时段 | `doctorId, date` | `[{timeSlot}]` |
| POST | `/api/appointment/book` | 预约挂号 | `{patientId, doctorId, slotId, symptoms}` | `{appointmentId}` |
| POST | `/api/appointment/cancel` | 取消预约 | `{appointmentId, reason}` | `{success}` |
| GET | `/api/appointment/detail` | 预约详情 | `appointmentId` | `{appointment}` |
| GET | `/api/appointment/list` | 预约列表 | `patientId` 或 `doctorId` | `[{appointment}]` |
| POST | `/api/payment/create` | 创建支付 | `{appointmentId, method}` | `{paymentId}` |
| GET | `/api/payment/status` | 支付状态 | `paymentId` | `{status}` |

**数据表：** `department`, `doctor`, `schedule`, `time_slot`, `appointment`, `payment`

**排班逻辑简化（阶段一）：**
```
每个医生排班 → 根据 slot_duration 自动生成 time_slot（如 30分钟/个）
患者预约 → 选择时段 → 锁定时段 → 支付 → 完成
超时 15 分钟未支付 → 自动释放时段（定时任务）
```

**前端页面：**

| 页面 | 路由 | 对应组件 | 说明 |
|------|------|---------|------|
| 科室选择 | `/appointment/dept` | DeptSelectView | 科室树形列表（内科/外科二级） |
| 医生选择 | `/appointment/doctor` | DoctorSelectView | 科室下医生列表 + 排班展示 |
| 预约确认 | `/appointment/confirm` | AppointmentConfirmView | 选择时段 + 症状输入 + 费用确认 |
| 支付页面 | `/appointment/pay/:id` | PaymentView | 支付方式选择（模拟） |
| 挂号记录 | `/appointment/records` | AppointmentListView | 我的挂号记录列表 |
| 排班管理 | `/admin/schedule` | ScheduleManageView | 管理员设置医生排班 |

---

### T7 M03 诊疗记录模块（Controller + Service + Repository + 前端页面）

**后端 API 清单：**

| 方法 | 路径 | 说明 | 请求参数 | 返回 |
|------|------|------|---------|------|
| POST | `/api/medical-record/create` | 创建病历 | `{patientId, doctorId, appointmentId, ...}` | `{recordId}` |
| GET | `/api/medical-record/list` | 病历列表 | `patientId` 或 `doctorId` | `[{record}]` |
| GET | `/api/medical-record/detail` | 病历详情 | `recordId` | `{medicalRecord}` |
| PUT | `/api/medical-record/update` | 更新病历 | `{recordId, ...}` | `{success}` |
| PUT | `/api/medical-record/complete` | 完成病历 | `{recordId}` | `{success}` |
| POST | `/api/prescription/create` | 开具处方 | `{recordId, items: [{drugId, dosage, ...}]}` | `{prescriptionId}` |
| GET | `/api/prescription/list` | 处方列表 | `recordId` | `[{prescription}]` |
| GET | `/api/prescription/detail` | 处方详情 | `prescriptionId` | `{prescription + items}` |
| POST | `/api/examination/create` | 开检查单 | `{recordId, examName, category, ...}` | `{orderId}` |
| GET | `/api/examination/list` | 检查单列表 | `recordId` | `[{examinationOrder}]` |
| GET | `/api/examination/result` | 检查结果 | `orderId` | `{examinationResult}` |

**数据表：** `medical_record`, `prescription`, `prescription_item`, `examination_order`, `examination_result`, `prescription_audit_log`

**前端页面：**

| 页面 | 路由 | 对应组件 | 说明 |
|------|------|---------|------|
| 待诊列表 | `/doctor/waiting` | WaitingListView | 医生查看当日预约患者列表 |
| 病历编辑 | `/doctor/record/:appointmentId` | MedicalRecordView | 病历表单（主诉、现病史、诊断等） |
| 处方开具 | `/doctor/prescription/:recordId` | PrescriptionView | 药品选择 + 处方明细表 |
| 检查单开单 | `/doctor/exam/:recordId` | ExamOrderView | 检查项目选择 + 开单 |
| 检查结果 | `/doctor/exam-result/:orderId` | ExamResultView | 查看检查报告 |
| 患者端病历 | `/patient/records` | PatientRecordView | 患者查看自己的病历和处方 |

**病历填写流程（阶段一）：**

```
医生进入待诊列表 → 点击接诊
  → 书写病历（主诉/现病史/诊断等字段）
  → 如需开药：进入处方开单页（选择药品、填写剂量频次）
  → 如需检查：进入检查开单页（选择检查项目）
  → 完成病历
```

---

### T8 前后端联调

**目标：** 覆盖完整诊疗流程

**联调场景：**
```
① 管理员登录系统 → 新增医生/管理员账号
② 管理员设置医生排班
③ 患者注册 → 患者登录
④ 患者预约挂号 → 选择科室/医生/时段 → 支付完成
⑤ 医生登录 → 查看待诊列表 → 接诊
⑥ 医生书写病历 → 开具处方
⑦ 患者查看挂号记录、病历、处方
```

**注意点：**
| 项目 | 处理方式 |
|------|---------|
| 跨域 | `vite.config.ts` proxy 代理 `/api` → `http://localhost:8080` |
| JWT 存储 | `localStorage` + Pinia |
| Token 携带 | Axios 请求拦截器自动注入 Header |
| Loading 状态 | Element Plus `v-loading` / ElMessage loading |
| 错误处理 | 响应拦截器统一弹窗提示 |

---

### T9 部署验证

```bash
# Linux 启动后端
cd /home/yuu/CloudBrain/backend
mvn spring-boot:run

# Linux 启动前端（开发模式）
cd /home/yuu/CloudBrain/frontend
npm run dev -- --host 0.0.0.0

# 访问前端：http://192.168.110.128:5173
# 访问 Swagger：http://192.168.110.128:8080/doc.html
```

---

## 三、阶段一 API 汇总

| 模块 | API 数量 |
|------|---------|
| M01 用户中心 | 9 个 |
| M08 系统管理 | 6 个 |
| M02 患者档案 | 5 个 |
| M06 预约管理 | 12 个 |
| M03 诊疗记录 | 11 个 |
| **总计** | **43 个 API** |

---

## 四、文件结构

```
CloudBrain/                          ← Linux /home/yuu/CloudBrain/
├── docker-compose.yml               # MySQL + Redis + MinIO
├── sql/
│   └── init/
│       ├── 00-init-db.sql           # 创建数据库
│       ├── 01-user.sql              # M01+M08 表（7张）
│       ├── 02-patient.sql           # M02 表（1张）
│       ├── 03-appointment.sql       # M06 表（6张）
│       ├── 04-medical.sql           # M03 表（6张）
│       ├── 05-ai.sql                # M04 表（5张）
│       ├── 06-image.sql             # M05 表（4张）
│       ├── 07-pharmacy.sql          # M07 表（5张）
│       └── 08-notify.sql            # 通知服务表（3张）
├── backend/                         # Spring Boot 3 项目（待建）
└── frontend/                        # Vue 3 项目（待建）
```

---

## 五、建议分工（3 人团队）

| 成员 | 负责模块 | 主要任务 |
|------|---------|---------|
| **余润东** | 基础设施 + 后端脚手架 + 预约管理后端 | T1, T2, T6(后端) — 10 个 API |
| **杨继鑫** | 前端脚手架 + 用户中心 + 系统管理 + 预约管理前端 | T3, T4(全部), T4.5(全部), T6(前端) — 6 个页面 |
| **刘鹏杰** | 患者档案 + 诊疗记录 + 联调 + 部署 |T7(全部), T8, T9 — 16 个 API + 9 个页面 |

---

## 六、阶段一后端审查与修复记录

> 审查日期：2026-06-22 | 审查范围：全部后端代码

### 6.1 已修复问题

| 编号 | 严重程度 | 问题描述 | 涉及文件 | 修复方式 |
|------|---------|---------|---------|---------|
| F1 | 中等 | `Schedule.availableSlots` 初始化为 `maxPatients` 且从不更新，字段实际失效 | `ScheduleServiceImpl.java` | `generateTimeSlots` 返回实际生成数，`availableSlots` 用实际时段数初始化 |
| F2 | 中等 | 预约时未校验医生可用性（`available`、`maxDailyPatients`） | `AppointmentServiceImpl.java` | `book()` 中新增：停诊医生拒绝预约、当日预约满拒绝预约 |
| F3 | 中等 | `availableSlots` 在预约/取消/过期释放时未联动更新 | `AppointmentServiceImpl.java` | 锁定时段时 `-1`，取消/过期释放时 `+1` |
| F4 | 中等 | Payment 和 Appointment 的 `paymentStatus` 编码不一致（支付成功时 Payment=2，Appointment=1） | `PaymentServiceImpl.java` | 统一为 `0=待支付 1=已支付 2=已退款`，Payment 创建时赋值改为 `1` |
| F5 | 中等 | `DoctorService.listByDepartment()` 不过滤停诊医生，与 `listAll()` 行为不一致 | `DoctorServiceImpl.java` | 增加 `.eq(Doctor::getAvailable, 1)` 过滤条件 |

### 6.2 已知遗留问题（二阶段处理）

| 编号 | 严重程度 | 问题描述 | 推迟原因 |
|------|---------|---------|---------|
| K1 | 严重 | 时段抢占存在竞态条件（查 slot 和更新 status 之间有窗口期） | 需要引入分布式锁（Redis setnx）或数据库行锁，涉及架构决策 |
| K2 | 严重 | 支付不验证预约归属权（任何人可为任意预约支付） | 需确认支付环节的权限模型后再改 |
| K3 | 严重 | 已支付预约取消不处理退款 | 需对接真实支付/退款流程后才能实现 |
| K4 | 严重 | 注册时 `userType` 由客户端传入，可自选管理员角色 | 需确认注册流程：是否关闭公开注册、是否改为邀请码/审核制 |
| K5 | 一般 | JWT 认证不写入 `GrantedAuthority`，角色权限未走标准注解 | 权限矩阵待二阶段确定后再串联 |
| K6 | 一般 | `SystemUserService` 创建医生时 `departmentId` 硬编码为 `"0"` | 暂作占位符，后续通过 `RoleAssign` 接口更新科室 |
| K7 | 一般 | CORS 配置 `*` + `credentials=true` 浏览器不兼容 | 部署时替换为具体域名 |
| K8 | 一般 | 无 Token 黑名单/登出机制、无接口限流 | 二阶段基础设施增强 |

### 6.3 修复影响范围

```
预约挂号流程（T6）:
  book()    → 新增医生可用性校验 + 日限额校验 + availableSlots 扣减
  cancel()  → 新增 availableSlots 回补

排班管理（T6）:
  create()  → availableSlots 改用实际时段数初始化

医生查询（T6）:
  listByDepartment() → 增加可用状态过滤，与 listAll() 行为统一

支付流程（T6）:
  create()  → paymentStatus 编码统一为 0/1/2
```
